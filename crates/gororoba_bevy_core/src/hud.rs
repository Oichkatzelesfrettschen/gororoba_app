// egui-based HUD overlay for runtime diagnostics and game UI.

use bevy::prelude::*;

pub struct HudPlugin;

impl Plugin for HudPlugin {
    fn build(&self, _app: &mut App) {
        // Phase 1: implement HUD overlay via bevy_egui.
    }
}
